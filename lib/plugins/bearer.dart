/// Bearer plugin: authenticate with an `Authorization: Bearer <token>` header
/// instead of cookies.
///
/// Requires the `bearer()` plugin on your Better Auth server.
library;

export "../src/plugins/bearer/bearer_plugin.dart";
