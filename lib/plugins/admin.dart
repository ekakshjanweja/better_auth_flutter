/// Admin plugin: user management, roles, bans, and impersonation.
///
/// Requires the `admin()` plugin on your Better Auth server. Importing this
/// library adds an `admin` getter to [BetterAuthClient].
library;

export "../src/plugins/admin/admin_plugin.dart";
