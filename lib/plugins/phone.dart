/// Phone number plugin: OTP sign-in, verification, and password reset.
///
/// Requires the `phoneNumber()` plugin on your Better Auth server. Importing
/// this library adds a `phone` getter to [BetterAuthClient].
library;

export "../src/plugins/phone/phone_plugin.dart";
